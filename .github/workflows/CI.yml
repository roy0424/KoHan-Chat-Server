name: CI/CD Pipeline

on:
  push:
    branches:
      - "**"
  pull_request:
  merge_group:

jobs:
  build:
    name: Build, Test, and Coverage
    runs-on: ubuntu-latest

    strategy:
      matrix:
        java-version: [21]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK ${{ matrix.java-version }}
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: ${{ matrix.java-version }}

    - name: Start MongoDB
      uses: supercharge/mongodb-github-action@1.11.0
      with:
        mongodb-version: latest
        mongodb-db: 'testdb'

    - name: Wait for MongoDB to start
      run: |
        sleep 5

    - name: Install MongoDB CLI
      run: |
        wget -qO - https://www.mongodb.org/static/pgp/server-4.4.asc | sudo apt-key add -
        echo "deb [ arch=amd64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/4.4 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-4.4.list
        sudo apt-get update
        sudo apt-get install -y mongodb-org-shell

    - name: Create MongoDB User
      run: |
        mongo --eval 'use admin; db.createUser({user: "testuser", pwd: "testpassword", roles: [{role: "readWrite", db: "testdb"}]})'

    - name: Create application-test.yml in all modules
      run: |
        modules=("authentication" "file" "message-rest" "push") # 하위 모듈의 이름들
        for module in "${modules[@]}"
        do
          mkdir -p $module/src/main/resources
          echo "spring:" > $module/src/main/resources/application.yml
          echo "  data:" >> $module/src/main/resources/application.yml
          echo "    mongodb:" >> $module/src/main/resources/application.yml
          echo "      uri: mongodb://testuser:testpassword@localhost:27017/testdb" >> $module/src/main/resources/application-test.yml
        done

    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          gradle-${{ runner.os }}-

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew build
